.favorite
  - if favorite = current_user.favorited(post)
    = link_to [topic, post, favorite], class: 'btn btn-small', method: :delete do
      %i.icon-star-empty>
      \&#160; Unfavorite
  - else
    = link_to [topic, post, Favorite.new], class: 'btn btn-small', method: :post do
      %i.icon-star>
      \&#160; Favorite
