%h1 Topics
.row
  .span8
    - @topics.each do |topic|
      .media
        .media-body
          %h4.media-heading 
            = link_to topic.name, topic
          %small 
            = topic.description
    = will_paginate @topics
  .span4
    - if can? :create, Topic
      = link_to "New Topic", new_topic_path, class: 'btn btn-large btn-blo'